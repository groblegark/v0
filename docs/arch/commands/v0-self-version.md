# v0-self-version

**Purpose:** Show version information.

## Usage

```bash
v0 self version          # Show current version
v0 self version --check  # Also check for updates
```

Shows version, channel (stable/nightly), install method, and path.
